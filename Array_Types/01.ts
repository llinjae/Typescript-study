// 대괄호 앞에 어떤타입인지 알려줘야함
const activeUsers: string[] = [];

activeUsers.push('Tony');

// error
// activeUsers.push(12);

const ageList: number[] = [45, 56, 13];
ageList[0] = 99;

// error
// ageList[0] = 'asd'
